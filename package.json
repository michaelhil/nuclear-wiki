{
  "name": "nuclear-wiki",
  "version": "1.0.0",
  "description": "LLM-wiki for nuclear AI research — MCP server and tooling",
  "type": "module",
  "scripts": {
    "mcp": "bun run src/mcp-server.ts",
    "wiki:serve": "mkdocs serve",
    "wiki:build": "mkdocs build",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}
